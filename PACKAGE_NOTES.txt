BINANCE BOT LIVE-CAPABLE RELEASE CANDIDATE

Purpose: real Binance Spot execution only after every promotion gate passes.
RELEASE_MODE=live, but .env.example intentionally starts in simulation with
BINANCE_TESTNET=true. This prevents an unpack-and-run action from risking money.

Before any live promotion:
1. Read README.md, docs/GITHUB_ORACLE_DEPLOYMENT.md and
   docs/SHARIA_LOCAL_SCREENING.md.
2. Put secrets only in GitHub environment secrets and /etc on Oracle.
3. Complete Binance Spot Testnet lifecycle and Oracle soak evidence first.
4. Run the complete Actions workflow and inspect its extracted-artifact tests.
5. Configure the protected GitHub oracle-production environment with reviewers.
6. Populate and independently verify the persistent Sharia source registry;
   the shipped empty registry intentionally authorizes no asset.
7. Generate signed live-promotion evidence for the exact installed release.
8. Change EXECUTION_MODE=live and BINANCE_TESTNET=false together, using a
   restricted Spot-only Binance key with withdrawals disabled and an IP allowlist.

This package is live-capable but ships disarmed. It is not externally certified,
not a profit claim, and not a religious ruling.
